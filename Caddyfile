:80 {
    # Serve static and media files directly from the shared volumes
    handle_path /static/* {
        root * /app/staticfiles
        file_server
    }
    
    handle_path /media/* {
        root * /app/media
        file_server
    }

    # Reverse proxy everything else to Django
    reverse_proxy * backend:8000
}
